---
interface Props {
  title: string;
  company: string;
  dates: string;
  summary: string;
}

const { title, company, dates, summary } = Astro.props;
---

<article class="py-3">
  <div class="flex flex-wrap items-baseline justify-between gap-x-4">
    <span>
      <span class="font-medium">{title}</span>
      <span class="text-muted"> · {company}</span>
    </span>
    <span class="text-sm text-muted">{dates}</span>
  </div>
  <p class="mt-1 text-muted text-sm leading-relaxed">{summary}</p>
</article>
